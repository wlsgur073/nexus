# Codex UX 설계 명세

> **버전**: 1.0
> **작성일**: 2026-03-20
> **목적**: 기존 27개 와이어프레임을 6대 비전 원칙에 따라 17개 화면으로 통합/재설계한 UX 명세
> **기반**: Nexus 독립 솔루션 레이아웃, 6대 비전 원칙

### 에이전트별 참조 가이드

| 에이전트               | 참조 섹션                                                                                              | 설명                                        |
| ---------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------- |
| **frontend-developer** | 화면 통합 요약, 2. 사용자 여정, 3. 정보 아키텍처, 4. 네비게이션, 5. 화면 명세(전체), 6. AI Data Butler | 17개 화면 레이아웃, 컴포넌트, 인터랙션 설계 |
| **code-reviewer**      | 1. 사용자 역할, 7. 공유 UX 패턴, 8. 접근성 & 반응형                                                    | UX 일관성 검증, 접근성 규칙, WCAG 기준      |
| **package-developer**  | 7. 공유 UX 패턴 (Badge 색상 시스템)                                                                    | 공유 컴포넌트의 시각적 규격 참조            |

---

## 화면 통합 요약 (27개 → 17개)

| #   | 신규 화면                        | 통합된 기존 화면                                                                                                                                                                                                  | 통합 이유                                                                                                               |
| --- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 1   | 로그인                           | `login.html`                                                                                                                                                                                                      | 유지 (독립 화면)                                                                                                        |
| 2   | 신청자 대시보드                  | `dashboard.html` + `my-requests.html`                                                                                                                                                                             | 역할 적응형 대시보드로 신청자 전용 뷰 통합. 내 신청 목록을 대시보드 위젯으로 흡수하여 별도 화면 제거                    |
| 3   | 승인자 대시보드                  | `dashboard.html` + `approval.html` (stat 카드)                                                                                                                                                                    | 승인자 전용 KPI와 승인 대기 큐를 대시보드에 통합. 승인 처리 진입점 역할                                                 |
| 4   | 통합 표준 탐색기                 | `standard-word-search.html` + `standard-domain-search.html` + `standard-term-search.html`                                                                                                                         | **핵심 통합**: 3개 검색 화면을 탭 기반 1개 화면으로 통합. 동일한 검색/필터 패턴을 공유하므로 분리 불필요                |
| 5   | 표준 상세 보기 & 인라인 거버넌스 | `standard-word-detail.html` + `standard-domain-detail.html` + `standard-term-detail.html` + `standard-word-apply.html` + `standard-domain-apply.html` + `standard-term-apply.html` + `request-delete-impact.html` | **핵심 통합**: 상세 보기 Sheet에서 바로 편집/신청. 6개 상세+신청 화면과 영향도 평가를 Sheet 내 인라인 워크플로우로 흡수 |
| 6   | 신규 표준 신청                   | `standard-word-apply.html` (신규 탭) + `standard-domain-apply.html` (신규 탭) + `standard-term-apply.html` (신규 탭)                                                                                              | 완전히 새로운 표준을 등록할 때만 사용. 변경/삭제 신청은 상세 Sheet의 인라인 거버넌스로 이동                             |
| 7   | 승인 워크벤치                    | `approval.html` + `approval-detail.html` + `application-list.html`                                                                                                                                                | 승인 처리 목록과 신청 상세를 하나의 워크벤치로 통합. 목록 선택 시 사이드 패널에서 상세 확인/처리                        |
| 8   | 검증 대시보드                    | `standard-validation.html`                                                                                                                                                                                        | 유지 (독립 화면)                                                                                                        |
| 9   | 검증 상세                        | `standard-validation-detail.html`                                                                                                                                                                                 | 유지 (독립 화면)                                                                                                        |
| 10  | 거버넌스 포털                    | `governance-portal.html`                                                                                                                                                                                          | 유지 (독립 화면)                                                                                                        |
| 11  | 감사 추적                        | `audit-trail.html`                                                                                                                                                                                                | 유지 (독립 화면)                                                                                                        |
| 12  | 공통코드 관리                    | `common-code.html`                                                                                                                                                                                                | 유지 (독립 화면)                                                                                                        |
| 13  | 공통코드 조회                    | `common-code-search.html`                                                                                                                                                                                         | 유지 (독립 화면)                                                                                                        |
| 14  | 사용자 관리                      | `user-management.html`                                                                                                                                                                                            | 유지 (독립 화면)                                                                                                        |
| 15  | 권한 관리                        | `menu-permission.html`                                                                                                                                                                                            | 유지 (독립 화면)                                                                                                        |
| 16  | 코드 관리 (시스템)               | `system-code.html`                                                                                                                                                                                                | 유지 (독립 화면)                                                                                                        |
| 17  | DB 연결 설정                     | `db-settings.html`                                                                                                                                                                                                | 유지 (독립 화면)                                                                                                        |

**제거된 화면**: `my-requests.html` (대시보드 흡수), `application-list.html` (승인 워크벤치 흡수), `index.html` (프레젠테이션용, 제품 불필요)

---

## 1. 사용자 역할 정의

### 1.1 역할별 특성

#### 신청자 (Requester)

- **대상**: 비즈니스 분석가, 개발자, 데이터 엔지니어
- **핵심 니즈**: 기존 표준을 빠르게 검색하고, 필요 시 신규/변경/삭제 신청을 최소 단계로 제출하는 것
- **주요 시나리오**:
  - 표준용어를 검색하여 물리 컬럼명 확인
  - 존재하지 않는 표준단어를 신규 신청
  - 기존 표준의 정의를 변경 신청
  - 불필요한 표준의 삭제 신청 (영향도 평가 포함)
  - 피드백 받은 신청 건을 수정/재신청
- **빈도**: 일 1-5회 검색, 주 1-3회 신청
- **Pain Point**: 검색과 신청이 분리되어 화면 전환이 잦음, 신청 상태 추적이 불편

#### 검토/승인자 (Reviewer/Approver)

- **대상**: 표준 관리자, 데이터 거버넌스 리더
- **핵심 니즈**: 대기 중인 신청 건을 효율적으로 검토하고 승인/반려/피드백 처리하는 것
- **주요 시나리오**:
  - 승인 대기 건수 확인 및 우선순위 파악
  - 변경 신청의 전/후 비교 확인
  - 일괄 승인/반려 처리
  - 검토 의견(피드백) 전달
- **빈도**: 일 3-10회 승인 처리
- **Pain Point**: 상세 확인을 위해 별도 화면 이동 필요, 맥락 전환 비용 높음

#### 표준 관리자 (STD_MANAGER)

- **대상**: 거버넌스 매니저, 데이터 품질 리더
- **핵심 니즈**: 조직 전체 표준 품질을 모니터링하고, 교차 도메인 일관성을 유지하는 것
- **주요 시나리오**:
  - 거버넌스 포털에서 준수율 모니터링
  - 검증 대시보드에서 위반 항목 추적
  - 공통코드 관리 및 거버넌스
  - 감사 추적 조회
- **빈도**: 일 1-2회 모니터링, 주 1-2회 집중 리뷰
- **Pain Point**: 분산된 모니터링 지표, 위반 → 시정 연결 어려움

#### 시스템관리자 (System Administrator)

- **대상**: IT 운영팀, 플랫폼 관리자
- **핵심 니즈**: 사용자 계정, 권한, 시스템 코드, DB 연결 등 운영 설정을 관리하는 것
- **주요 시나리오**:
  - 사용자 계정 생성/비활성화
  - 역할별 메뉴 권한 설정
  - 시스템 코드 관리 (보호 코드 정책)
  - DB 연결/SSH 터널링 설정
- **빈도**: 주 1-2회 (이벤트 기반)
- **Pain Point**: 관리 화면 접근 빈도 낮아 UI 숙지가 어려움

#### 조회전용 (Read-only User)

- **대상**: 인증된 제한 권한 사용자
- **핵심 니즈**: 표준 정보를 검색하고 참조하는 것
- **주요 시나리오**:
  - 표준용어 검색 및 물리명 확인
  - 공통코드 조회
  - 검증 대시보드 현황 확인
- **빈도**: 일 1-3회 검색
- **Pain Point**: 사용 가능 기능 범위를 명확히 인지하기 어려움

---

## 2. 사용자 여정 맵 (User Journey Map)

### 2.1 신청자 — 표준 검색 → 변경 신청 플로우

```markdown
[로그인]
│
▼
[신청자 대시보드]──────────────────────────────────────────┐
│ │
│ "승인대기 2건" 알림 확인 │
│ "피드백대기 1건" → 클릭 시 해당 신청 Sheet 열림 │
│ │
▼ │
[통합 표준 탐색기] │
│ │
│ 탭: 표준용어 선택 │
│ 검색: "고객번호" 입력 │
│ AI Data Butler: "고객번호(CUST_NO)" 자동완성 추천 │
│ │
▼ │
[검색 결과 행 클릭] │
│ │
▼ │
[표준 상세 Sheet 열림 (화면 우측 슬라이드)] │
│ │
│ 기본 정보 확인 │
│ "편집" 버튼 클릭 → 인라인 편집 모드 전환 │
│ 정의 필드 수정 │
│ "변경 신청 제출" → 확인 Dialog │
│ │
▼ │
[Toast: "변경 신청이 제출되었습니다 (REQ-2026-0343)"] │
│ │
└────────────────────────────────────────────────────────┘
대시보드의 "내 신청" 위젯에 신규 건 반영
```

### 2.2 신청자 — 신규 표준 등록 플로우

```markdown
[통합 표준 탐색기]
│
│ 검색: "결제수단코드" → 결과 없음
│ AI Data Butler: "유사 표준 3건 발견" 패널 표시
│ "그래도 신규 신청" 버튼 클릭
│
▼
[신규 표준 신청 화면]
│
│ 유형 선택: 표준용어
│ 폼 입력: 용어명*, 도메인유형*, 인포타입*, 정의*
│ 물리명 자동 생성: PAY_MTHD_CD
│ AI Data Butler 미니패널: 유사 표준 3건 표시
│
│ ┌─ [초안 저장] ─── Draft로 저장, 나중에 제출 가능
│ │
│ └─ [신청 제출] ─── 바로 신청 (PENDING 상태)
│
▼
[Toast: "신청이 제출되었습니다"]
[대시보드로 자동 이동 또는 현재 화면 유지 선택]
```

### 2.3 승인자 — 승인 처리 플로우

```markdown
[로그인]
│
▼
[승인자 대시보드]
│
│ "승인 대기 5건" KPI 카드 클릭
│ 또는 "미처리 12일 경과 1건" 긴급 알림 클릭
│
▼
[승인 워크벤치]
│
│ 좌: 신청 목록 (필터: 구분, 요청구분)
│ 우: 선택된 신청의 상세 패널
│
│ 상세 패널에서:
│ - 변경 비교 테이블 (현재값 vs 변경요청값, 하이라이트)
│ - 신청 사유 확인
│ - 인라인 코멘트: 특정 필드에 의견 작성
│ - 처리 이력 타임라인
│
│ ┌─ [승인] + 처리사유 입력 → 표준 원장 반영
│ ├─ [반려] + 반려사유 입력 → 신청자에게 알림
│ └─ [검토요청] + 피드백 내용 입력 → 신청자에게 수정 요청
│
│ 일괄 처리: 체크박스 선택 → "일괄 승인" / "일괄 반려"
│
▼
[Toast: "3건 일괄 승인 처리되었습니다"]
[대시보드 KPI 자동 갱신]
```

### 2.4 표준 관리자 — 품질 모니터링 플로우

```markdown
[거버넌스 포털]
│
│ 준수율 게이지 확인
│ "표준용어 준수율 79%" → 하락 확인
│
▼
[검증 대시보드] (사이드바 또는 링크로 이동)
│
│ 위반 127건 확인
│ "명명규칙 위반 54건" 클릭
│
▼
[검증 상세] (명명규칙 탭 활성)
│
│ 위반 항목 목록 확인
│ "payAmt → PAY_AMT 권장" 확인
│ 체크박스 선택 → "일괄 시정 신청"
│
▼
[변경 신청 자동 생성, Toast 확인]
[감사 추적에서 시정 이력 확인]
```

### 2.5 시스템관리자 — 사용자/권한 관리 플로우

```markdown
[사용자 관리]
│
│ "사용자 추가" 클릭 → Dialog
│ ID, 이름, 비밀번호, 역할, 부서, 이메일 입력
│ "저장" → 사용자 생성
│
▼
[권한 관리]
│
│ 역할 탭 선택 (시스템관리자 / 검토승인자 / 표준 관리자 / 신청자 / 조회전용)
│ 메뉴 트리에서 접근 허용 메뉴 선택
│ CRUD 체크박스 조정
│ "저장" → 권한 반영
```

---

## 3. 정보 아키텍처 (IA)

### 3.1 전체 메뉴 구조

```markdown
Codex
├── 대시보드 (/solutions/codex)
│ ├── [신청자 뷰] — 역할에 따라 자동 전환
│ └── [승인자 뷰] — 역할에 따라 자동 전환
│
├── 표준 관리
│ ├── 통합 표준 탐색기 (/solutions/codex/standards)
│ │ ├── [표준용어 탭]
│ │ ├── [표준단어 탭]
│ │ └── [표준도메인 탭]
│ ├── 신규 표준 신청 (/solutions/codex/standards/new)
│ └── 공통코드 조회 (/solutions/codex/common-codes)
│
├── 거버넌스
│ ├── 승인 워크벤치 (/solutions/codex/approvals)
│ ├── 거버넌스 포털 (/solutions/codex/governance)
│ └── 감사 추적 (/solutions/codex/audit)
│
├── 품질 관리
│ ├── 검증 대시보드 (/solutions/codex/validations)
│ └── 검증 상세 (/solutions/codex/validations/[executionId])
│
├── 관리 (시스템관리자 전용)
│ ├── 공통코드 관리 (/solutions/codex/admin/common-codes)
│ ├── 사용자 관리 (/solutions/codex/admin/users)
│ ├── 권한 관리 (/solutions/codex/admin/permissions)
│ ├── 코드 관리 (/solutions/codex/admin/system-codes)
│ └── DB 연결 설정 (/solutions/codex/admin/db-settings)
│
└── [전역 요소]
├── Command Palette (Ctrl+K)
├── 알림 센터 (헤더 벨 아이콘)
└── AI Data Butler (각 화면 통합)
```

### 3.2 메뉴 깊이

| 깊이  | 예시                                            | 비고                                 |
| ----- | ----------------------------------------------- | ------------------------------------ |
| 1단계 | 대시보드, 표준 관리, 거버넌스, 품질 관리, 관리  | 사이드바 섹션 헤딩                   |
| 2단계 | 통합 표준 탐색기, 승인 워크벤치, 사용자 관리 등 | 사이드바 링크                        |
| 3단계 | 표준 상세 Sheet, 검증 상세, 신규 신청           | 2단계 내 진입 (Sheet 또는 별도 경로) |

최대 깊이 3단계로 제한. 3단계 이상의 중첩 네비게이션은 Sheet 또는 Dialog로 처리하여 사용자의 컨텍스트 이탈을 방지한다.

### 3.3 역할별 접근 권한

| 메뉴             | 시스템관리자 | 검토/승인자 | 표준 관리자 | 신청자 | 조회전용 |
| ---------------- | :----------: | :---------: | :---------: | :----: | :------: |
| 대시보드         |      O       |      O      |      O      |   O    |    O     |
| 통합 표준 탐색기 |      O       |      O      |      O      |   O    |    O     |
| 신규 표준 신청   |      O       |      -      |      O      |   O    |    -     |
| 공통코드 조회    |      O       |      O      |      O      |   O    |    O     |
| 승인 워크벤치    |      O       |      O      |      -      |   -    |    -     |
| 거버넌스 포털    |      O       |      O      |      O      |   -    |    -     |
| 감사 추적        |      O       |      O      |      O      |   O    |    -     |
| 검증 대시보드    |      O       |      O      |      O      |   O    |    O     |
| 검증 상세        |      O       |      O      |      O      |   O    |    -     |
| 공통코드 관리    |      O       |      -      |      O      |   -    |    -     |
| 사용자 관리      |      O       |      -      |      -      |   -    |    -     |
| 권한 관리        |      O       |      -      |      -      |   -    |    -     |
| 코드 관리        |      O       |      -      |      -      |   -    |    -     |
| DB 연결 설정     |      O       |      -      |      -      |   -    |    -     |

---

## 4. 네비게이션 설계

### 4.1 사이드바 구조

Nexus PlatformShell의 Sidebar를 활용한다. Codex 진입 시 사이드바가 Codex 전용 메뉴로 전환된다.

```markdown
┌─────────────────────────┐
│ ← Nexus Codex │ ← 상단: Nexus 복귀 링크 + 솔루션명
├─────────────────────────┤
│ │
│ 📊 대시보드 │ ← 단일 링크 (역할별 뷰 자동 전환)
│ │
│ ── 표준 관리 ── │ ← 섹션 헤딩 (nav-section)
│ 🔍 표준 탐색기 │
│ ➕ 신규 신청 │ ← 신청 권한 없는 역할에게 숨김
│ 📋 공통코드 조회 │
│ │
│ ── 거버넌스 ── │ ← 섹션 헤딩
│ ✅ 승인 워크벤치 (5) │ ← 미처리 건수 Badge
│ 📈 거버넌스 포털 │
│ 📜 감사 추적 │
│ │
│ ── 품질 관리 ── │ ← 섹션 헤딩
│ 🔬 검증 대시보드 │
│ │
│ ── 관리 ── │ ← 시스템관리자에게만 표시
│ ⚙️ 공통코드 관리 │
│ 👥 사용자 관리 │
│ 🔑 권한 관리 │
│ 📝 코드 관리 │
│ 🔌 DB 연결 설정 │
│ │
├─────────────────────────┤
│ 👤 김데이터 │ ← 하단: 사용자 정보
│ 표준 관리자 │ 역할 표시
│ [로그아웃] │
└─────────────────────────┘
```

**사이드바 규칙**:

- `active`/`beta` 상태 메뉴만 표시
- 역할 기반 메뉴 필터링 (접근 권한 없는 메뉴 숨김)
- 승인 워크벤치에 미처리 건수 Badge 실시간 표시
- 현재 활성 메뉴 하이라이트 (좌측 accent border + 배경색)
- 섹션 헤딩은 클릭 불가, 시각적 구분자 역할

### 4.2 Breadcrumb 규칙

Nexus PlatformShell의 Breadcrumbs 컴포넌트를 활용한다.

| 경로                                      | Breadcrumb                                  |
| ----------------------------------------- | ------------------------------------------- |
| `/solutions/codex`                        | Nexus > Codex > 대시보드                    |
| `/solutions/codex/standards`              | Nexus > Codex > 통합 표준 탐색기            |
| `/solutions/codex/standards` + Sheet 열림 | Nexus > Codex > 통합 표준 탐색기 > {표준명} |
| `/solutions/codex/standards/new`          | Nexus > Codex > 신규 표준 신청              |
| `/solutions/codex/approvals`              | Nexus > Codex > 승인 워크벤치               |
| `/solutions/codex/governance`             | Nexus > Codex > 거버넌스 포털               |
| `/solutions/codex/audit`                  | Nexus > Codex > 감사 추적                   |
| `/solutions/codex/validations`            | Nexus > Codex > 검증 대시보드               |
| `/solutions/codex/validations/[id]`       | Nexus > Codex > 검증 대시보드 > 검증 상세   |
| `/solutions/codex/admin/users`            | Nexus > Codex > 관리 > 사용자 관리          |
| `/solutions/codex/admin/db-settings`      | Nexus > Codex > 관리 > DB 연결 설정         |

**규칙**:

- "Nexus"는 항상 첫 번째 (클릭 시 Nexus Command Center로 이동)
- "Codex"는 항상 두 번째 (클릭 시 Codex 대시보드로 이동)
- Sheet가 열린 경우 Sheet 대상명을 마지막 항목으로 추가 (클릭 불가, 현재 위치 표시)
- 최대 4단계까지만 표시

### 4.3 Command Palette (Ctrl+K)

shadcn/ui의 `Command` 컴포넌트를 활용한 전역 단축키 기반 네비게이션.

**트리거**: `Ctrl+K` (Windows) / `Cmd+K` (Mac), 또는 헤더의 검색 아이콘 클릭

**구성**:

```markdown
┌──────────────────────────────────────────┐
│ 🔍 명령어를 입력하세요... Ctrl+K │
├──────────────────────────────────────────┤
│ │
│ 최근 검색 │
│ 고객번호 (표준용어) │
│ CUST_NO (표준단어) │
│ │
│ 빠른 이동 │
│ 대시보드 │
│ 표준 탐색기 │
│ 승인 워크벤치 │
│ 거버넌스 포털 │
│ │
│ 액션 │
│ 신규 표준 신청 │
│ 검증 실행 │
│ │
│ 표준 검색 (입력 시 실시간) │
│ "고객" 입력 → 고객번호, 고객명, ... │
│ │
└──────────────────────────────────────────┘
```

**기능**:

- **빠른 이동**: 메뉴명 입력으로 즉시 이동 (fuzzy matching)
- **표준 검색**: 표준명/영문약어/물리명으로 실시간 검색 → 클릭 시 상세 Sheet 열림
- **액션**: "신규 신청", "검증 실행" 등 직접 액션 실행
- **최근 검색**: 최근 검색한 표준 5건 표시
- **AI 통합**: 검색어 입력 시 AI Data Butler가 유사 표준 추천 인라인 표시

---

## 5. 화면 명세

### 5.1 로그인

**목적**: Codex 시스템 접근을 위한 사용자 인증
**진입 경로**: 비인증 상태에서 모든 URL 접근 시 자동 리다이렉트
**경로**: `/solutions/codex/login`

**레이아웃**: Split-screen (PlatformShell 미적용, 사이드바 없음)

```markdown
┌───────────────────────────┬────────────────────────┐
│ │ │
│ 좌측 (55%) │ 우측 (45%) │
│ 브랜딩 영역 │ 로그인 폼 │
│ │ │
│ ┌────────────────┐ │ ┌────────────────┐ │
│ │ Codex 로고 │ │ │ 사용자 아이디 │ │
│ │ Enterprise │ │ │ [__________] │ │
│ │ Standard │ │ │ │ │
│ │ Platform │ │ │ 비밀번호 │ │
│ └────────────────┘ │ │ [__________] │ │
│ │ │ │ │
│ ┌──┐ ┌──┐ ┌──┐ │ │ [로그인 버튼] │ │
│ │통제│ │감사│ │역할│ │ │ │ │
│ │거버│ │추적│ │기반│ │ │ 환경설정 | 지원 │ │
│ └──┘ └──┘ └──┘ │ └────────────────┘ │
│ │ │
└───────────────────────────┴────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트  | shadcn/ui 매핑              | 설명                                       |
| --------- | --------------------------- | ------------------------------------------ |
| 로그인 폼 | `Card` + `Input` + `Button` | 사용자 아이디/비밀번호 입력                |
| 피처 카드 | `Card` (3-column grid)      | 통제된 거버넌스, 감사 추적, 역할 기반 접근 |
| 하단 링크 | 텍스트 링크                 | 환경설정(DB 설정), 지원 요청               |

**상태별 UI**:

| 상태              | 표시                                                              |
| ----------------- | ----------------------------------------------------------------- |
| Loading           | 로그인 버튼 Spinner + 비활성화                                    |
| Error (인증 실패) | 폼 상단 적색 Alert: "아이디 또는 비밀번호가 올바르지 않습니다"    |
| Error (서버 오류) | 적색 Alert: "서버 연결에 실패했습니다. 잠시 후 다시 시도해주세요" |

**역할별 차이점**: 없음 (로그인 전이므로 동일)

**AI Data Butler**: 미적용 (로그인 전)

---

### 5.2 신청자 대시보드

**목적**: 신청자 관점에서 본인 신청 현황과 표준 시스템 요약을 한눈에 파악
**진입 경로**: 로그인 후 자동 이동 (역할이 신청자/조회전용인 경우)
**경로**: `/solutions/codex` (역할별 뷰 자동 전환)

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "대시보드" [알림 🔔(3)] [Ctrl+K 검색] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│ │내 신청│ │승인대기│ │피드백 │ │반려 │ │
│ │ 12건 │ │ 3건 │ │ 1건 │ │ 2건 │ │
│ └──────┘ └──────┘ └──────┘ └──────┘ │
│ (내 신청 Stat 카드 — 클릭 시 아래 테이블 필터링) │
│ │
│ ┌─────────────────────┬────────────────────────┐ │
│ │ 내 최근 신청 (좌측) │ 시스템 현황 (우측) │ │
│ │ │ │ │
│ │ REQ-0343 표준용어 │ 표준단어 2,847건 │ │
│ │ "결제수단코드" │ ↑+124 (월대비) │ │
│ │ 신규 | 승인대기 │ │ │
│ │ 2026-03-19 │ 표준도메인 156건 │ │
│ │ [상세] [취소] │ ↑+8 (월대비) │ │
│ │ │ │ │
│ │ REQ-0342 표준단어 │ 표준용어 4,521건 │ │
│ │ "결제수단" │ ↑+203 (월대비) │ │
│ │ 신규 | 피드백대기 │ │ │
│ │ [수정/재신청] [상세] │ ┌──────────────────┐ │ │
│ │ │ │ 표준 등록 추이 차트 │ │ │
│ │ ... │ │ (6개월 바 차트) │ │ │
│ │ │ └──────────────────┘ │ │
│ │ "전체 보기 →" │ │ │
│ └─────────────────────┴────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 최근 활동 타임라인 │ │
│ │ ● 09:30 [승인] 김검토 — "고객번호" 승인 처리 │ │
│ │ ● 09:15 [신청] 나 — "결제수단코드" 신규 신청 │ │
│ │ ● 어제 [반려] 박승인 — "거래유형" 반려 │ │
│ │ "전체 이력 보기 →" │ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트           | shadcn/ui 매핑         | 설명                                                      |
| ------------------ | ---------------------- | --------------------------------------------------------- |
| 내 신청 Stat 카드  | `Card` (4-column grid) | 전체/승인대기/피드백/반려 건수. 클릭 시 아래 목록 필터링  |
| 내 최근 신청 목록  | `Card` > 커스텀 리스트 | 최근 5건 표시, 상태별 액션 버튼 (취소, 수정/재신청, 상세) |
| 시스템 현황 카드   | `Card` (3-row)         | 표준단어/도메인/용어 총 건수, 월대비 증감                 |
| 등록 추이 차트     | `Card` > 바 차트       | 6개월 등록 추이 (recharts 또는 유사 라이브러리)           |
| 최근 활동 타임라인 | `Card` > 타임라인      | 최근 5건, 시간순 정렬, 액션 배지                          |

**상태별 UI**:

| 상태              | 표시                                                                 |
| ----------------- | -------------------------------------------------------------------- |
| Loading           | Stat 카드 Skeleton, 목록 Skeleton (3행)                              |
| Empty (신청 없음) | "아직 신청한 표준이 없습니다. [신규 표준 신청하기 →]" 일러스트 + CTA |
| Error             | "대시보드 데이터를 불러오지 못했습니다. [다시 시도]" Alert           |

**역할별 차이점**:

- **조회전용 사용자**: 내 신청 영역 숨김, 시스템 현황 + 최근 활동만 표시
- **표준 관리자**: 내 신청 영역 + 거버넌스 요약 위젯 추가 (준수율 게이지 미니)

**AI Data Butler**: 대시보드 우측 하단에 플로팅 버튼. 클릭 시 "오늘 승인된 표준 3건", "피드백 대기 중인 신청 1건에 대한 수정 제안" 등 맥락 기반 알림 표시.

---

### 5.3 승인자 대시보드

**목적**: 승인자 관점에서 승인 대기 현황과 처리 실적을 집중 표시
**진입 경로**: 로그인 후 자동 이동 (역할이 검토/승인자인 경우)
**경로**: `/solutions/codex` (역할별 뷰 자동 전환)

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "대시보드" [알림 🔔(5)] [Ctrl+K 검색] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│ │신청대기│ │검토 중│ │금일처리│ │검토요청│ │
│ │ 5건 │ │ 2건 │ │ 3건 │ │ 3건 │ │
│ └──────┘ └──────┘ └──────┘ └──────┘ │
│ (승인 Stat 카드 — "신청대기" 클릭 시 워크벤치 이동) │
│ │
│ ┌─────────────────────┬────────────────────────┐ │
│ │ 긴급 승인 큐 (좌측) │ 승인 KPI (우측) │ │
│ │ │ │ │
│ │ ⚠️ 12일 경과 │ 처리율 94.2% │ │
│ │ REQ-0331 표준단어 │ ████████████░░ 목표95% │ │
│ │ "고객유형코드" 변경 │ │ │
│ │ 신청자: 이분석 │ 평균 승인소요 2.3일 │ │
│ │ [바로 처리 →] │ ████████░░░░░ 목표3일 │ │
│ │ │ │ │
│ │ ⏳ 5일 경과 │ 반려율 8.7% │ │
│ │ REQ-0338 표준용어 │ ██░░░░░░░░░░ 전월-1.2% │ │
│ │ "결제금액" 신규 │ │ │
│ │ [바로 처리 →] │ 이번 주 처리 12건 │ │
│ │ │ ████████████████ +3 │ │
│ │ "전체 승인 큐 →" │ │ │
│ └─────────────────────┴────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 최근 처리 이력 │ │
│ │ ● 09:30 [승인] "고객번호" — 신규 신청 승인 │ │
│ │ ● 09:15 [반려] "거래유형" — 명칭 중복 사유 │ │
│ │ ● 어제 [검토요청] "결제수단" — 정의 보완 요청 │ │
│ │ "전체 이력 보기 →" │ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트       | shadcn/ui 매핑         | 설명                                                                    |
| -------------- | ---------------------- | ----------------------------------------------------------------------- |
| 승인 Stat 카드 | `Card` (4-column grid) | 신청대기/검토중/금일처리/검토요청. "신청대기" 클릭 → 승인 워크벤치 이동 |
| 긴급 승인 큐   | `Card` > 커스텀 리스트 | 경과일 기준 내림차순, 최대 5건. 경고 아이콘으로 긴급도 표시             |
| 승인 KPI       | `Card` > Progress bar  | 처리율, 평균 승인소요, 반려율, 이번 주 처리                             |
| 최근 처리 이력 | `Card` > 타임라인      | 최근 5건, 시간순 정렬                                                   |

**상태별 UI**:

| 상태                 | 표시                                        |
| -------------------- | ------------------------------------------- |
| Loading              | Stat 카드 Skeleton, 큐 Skeleton             |
| Empty (대기 건 없음) | "처리할 승인 건이 없습니다" 아이콘 + 메시지 |
| Error                | Alert 컴포넌트로 오류 메시지 표시           |

**역할별 차이점**: 이 화면은 승인자 역할에게만 표시된다.

**AI Data Butler**: "이 신청은 기존 '고객유형' 표준과 85% 유사합니다. 중복 여부를 확인해주세요." 같은 승인 보조 인사이트를 긴급 큐 항목에 인라인 태그로 표시.

---

### 5.4 통합 표준 탐색기

**목적**: 표준단어, 표준도메인, 표준용어를 하나의 화면에서 탭으로 전환하며 검색/탐색
**진입 경로**: 사이드바 "표준 탐색기", Command Palette 검색, 대시보드 링크
**경로**: `/solutions/codex/standards`

**레이아웃**: PlatformShell > Main Content (탭 + 테이블 + 우측 Sheet)

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "통합 표준 탐색기" [신규 신청 +] [Ctrl+K] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌─────────┬─────────┬──────────┐ │
│ │ 표준용어 │ 표준단어 │ 표준도메인 │ ← Tabs │
│ │ (4,521) │ (2,847) │ (156) │ │
│ └─────────┴─────────┴──────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 🔍 [검색어 입력_______________] [상태 ▾] [도메│ │
│ │ AI: "고객" → 고객번호, 고객명, 고객유형코드 │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ No │ 표준용어 │ 물리명 │ 도메인 │ 인포 │ │
│ │ │ │ │ 유형 │ 타입 │ │
│ ├────┼────────────┼────────────┼───────┼──────┤ │
│ │ 1 │ 고객번호 │ CUST_NO │ 문자열 │ 코드 │ │
│ │ │ │ │ 기존● │ │ │
│ ├────┼────────────┼────────────┼───────┼──────┤ │
│ │ 2 │ 고객명 │ CUST_NM │ 문자열 │사용자│ │
│ │ │ │ │ 기존● │ 명 │ │
│ ├────┼────────────┼────────────┼───────┼──────┤ │
│ │ 3 │ 고객유형코드 │ CUST_TP_CD│ 코드 │ 코드 │ │
│ │ │ │ │ 신청◐ │ │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ◀ 1 2 3 ... 453 ▶ 10건/페이지 총 4,521건 │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트       | shadcn/ui 매핑        | 설명                                                              |
| -------------- | --------------------- | ----------------------------------------------------------------- |
| 유형 탭        | `Tabs`                | 표준용어/표준단어/표준도메인 전환. 각 탭에 총 건수 Badge          |
| 검색 바        | `Input` (search type) | 키워드 입력. 타이핑 시 AI 자동완성 드롭다운                       |
| 필터 드롭다운  | `Select` (복수)       | 상태 (전체/기존/신청/삭제), 도메인유형, 인포타입 (탭별 필터 상이) |
| 결과 테이블    | `Table`               | 탭별 컬럼 구성 변경 (아래 상세)                                   |
| 행 클릭        | → `Sheet` 열림        | 해당 표준의 상세 보기 Sheet (5.5) 우측 슬라이드                   |
| 페이지네이션   | 커스텀 Pagination     | 10건/페이지                                                       |
| 신규 신청 버튼 | `Button` (primary)    | Topbar 우측, 신규 표준 신청 화면으로 이동                         |

**탭별 테이블 컬럼**:

| 표준용어 탭   | 표준단어 탭     | 표준도메인 탭     |
| ------------- | --------------- | ----------------- |
| No            | No              | No                |
| 표준용어      | 표준단어        | 도메인명          |
| 물리명 (mono) | 영문약어 (mono) | 도메인유형        |
| 도메인유형    | 영문명          | 데이터타입 (mono) |
| 인포타입      | 정의 (말줄임)   | 데이터길이 (mono) |
| 정의 (말줄임) | 도메인유형      | 정의 (말줄임)     |
| 상태 (Badge)  | 상태 (Badge)    | 상태 (Badge)      |
| 등록일        | 등록일          | 등록일            |

**상태별 UI**:

| 상태                   | 표시                                                                                         |
| ---------------------- | -------------------------------------------------------------------------------------------- |
| Loading                | 테이블 Skeleton (5행), 탭/필터 비활성화                                                      |
| Empty (검색 결과 없음) | "검색 결과가 없습니다" 아이콘 + "AI Data Butler에게 물어보기" CTA + "신규 표준 신청하기" CTA |
| Empty (데이터 없음)    | "등록된 {유형명}이 없습니다. [신규 신청하기 →]"                                              |
| Error                  | Alert: "데이터를 불러오지 못했습니다. [다시 시도]"                                           |

**역할별 차이점**:

- **조회전용**: "신규 신청" 버튼 숨김, 행 클릭 시 상세 Sheet 읽기 전용
- **신청자/표준 관리자**: 전체 기능 사용 가능
- **승인자**: 검색/조회 가능, 상세 Sheet에서 신청 버튼 숨김

**AI Data Butler 통합**:

- **검색 자동완성**: 검색어 입력 시 AI가 유사 표준명/영문약어/물리명 실시간 추천 (Command 드롭다운)
- **Empty State 추천**: 검색 결과 없을 때 "혹시 이것을 찾으셨나요?" 3건 추천
- **유사 표준 경고**: 신규 신청 버튼 클릭 전, 검색어와 유사한 기존 표준이 있으면 인라인 안내

---

### 5.5 표준 상세 보기 & 인라인 거버넌스

**목적**: 통합 표준 탐색기에서 행 클릭 시 열리는 상세 보기. 인라인 편집으로 변경/삭제 신청까지 한 화면에서 처리
**진입 경로**: 통합 표준 탐색기에서 행 클릭, Command Palette 검색 결과 클릭, 다른 화면의 표준명 링크 클릭
**경로**: URL 변경 없이 Sheet 오버레이 (query param `?detail={id}&type={word|domain|term}`)

**레이아웃**: 우측 슬라이드 Sheet (너비 640px, 탐색기 위에 오버레이)

```markdown
┌──────────────────────┬───────────────────────────────┐
│ │ Sheet: 고객번호 (CUST_NO) ✕ │
│ (탐색기 배경, │ │
│ 어두워짐) │ [읽기 모드] ↔ [편집 모드] │
│ │ │
│ │ ── 기본 정보 ── │
│ │ 표준용어 고객번호 │
│ │ 물리명 CUST_NO │
│ │ 도메인유형 문자열 │
│ │ 인포타입 코드 │
│ │ 정의 고객을 식별하는 │
│ │ 고유 번호 │
│ │ 상태 ● 기존 │
│ │ 등록일 2025-08-15 │
│ │ 최종수정일 2026-01-20 │
│ │ │
│ │ ── 관련 표준 ── │
│ │ 구성 단어: │
│ │ 고객(CUST) ● 기존 │
│ │ 번호(NO) ● 기존 │
│ │ 사용 도메인: │
│ │ 문자열 / VARCHAR(20) │
│ │ │
│ │ ── 변경 이력 ── │
│ │ ● 2026-01-20 변경승인 박승인 │
│ │ ● 2025-08-15 신규승인 김관리 │
│ │ │
│ │ ┌────────────────────────┐ │
│ │ │ [변경 신청] [삭제 신청] │ │
│ │ └────────────────────────┘ │
│ │ │
└──────────────────────┴───────────────────────────────┘
```

**인라인 편집 모드 (변경 신청)**:

```markdown
┌───────────────────────────────┐
│ Sheet: 고객번호 — 편집 모드 ✕ │
│ │
│ ── 기본 정보 (편집 가능) ── │
│ 표준용어 [고객번호_________] │
│ 물리명 CUST_NO (readonly) │
│ 도메인유형 [문자열 ▾] │
│ 인포타입 [코드 ▾] │
│ 정의 [고객을 식별하는 │
│ 고유 번호______] │
│ │
│ ── 변경 미리보기 ── │
│ ┌────────┬────────┬────────┐ │
│ │ 항목 │ 현재값 │ 변경값 │ │
│ ├────────┼────────┼────────┤ │
│ │ 정의 │ 고객을 │ 고객을 │ │
│ │ │ 식별하는│ 고유하게│ │
│ │ │ 고유번호│ 식별하는│ │
│ │ │ │ 관리번호│ │
│ └────────┴────────┴────────┘ │
│ \* 변경된 필드만 표시 │
│ │
│ ── 신청 정보 ── │
│ 신청사유 [정의 명확화를 위해 │
│ 변경 신청합니다___] │
│ │
│ ┌────────────────────────┐ │
│ │ [초안 저장] [변경 신청 제출]│ │
│ └────────────────────────┘ │
│ │
└───────────────────────────────┘
```

**삭제 신청 모드 (Sheet 내 스텝 전환)**:

```markdown
Sheet 내에서 Stepper로 삭제 영향도 평가 진행:

Step 1/4: 영향 받는 시스템
☑ 운영DB ☑ DW ☐ 보고서 ☐ API ☐ 외부연동 ☐ 기타
기타 영역: [________________]

Step 2/4: 영향도 수준
○ 높음 ● 중간 ○ 낮음
설명\*: [영향도 설명________________]

Step 3/4: 대체 표준 제안
대체 표준: [검색____________] (AI 추천 인라인)
마이그레이션 계획: [________________]

Step 4/4: 삭제 사유
삭제 사유\*: [________________]

[취소] [◀ 이전] [삭제 신청 제출]
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트      | shadcn/ui 매핑               | 설명                                                                |
| ------------- | ---------------------------- | ------------------------------------------------------------------- |
| 상세 패널     | `Sheet` (side: right, 640px) | 우측 슬라이드. ESC 또는 오버레이 클릭으로 닫기                      |
| 모드 전환     | `Tabs` 또는 토글             | 읽기 모드 ↔ 편집 모드 전환                                          |
| 기본 정보     | `Card` 내 key-value 레이아웃 | 읽기 모드: 텍스트 표시, 편집 모드: Input/Select/Textarea            |
| 관련 표준     | `Card` > 미니 `Table`        | 구성 단어, 사용 도메인 정보                                         |
| 변경 이력     | `Card` > 타임라인            | 시간순 정렬, 최대 10건                                              |
| 변경 미리보기 | `Table` (3-column diff)      | 변경된 필드만 표시, 변경값 하이라이트                               |
| 삭제 Stepper  | 커스텀 Stepper               | 4단계 영향도 평가                                                   |
| 인라인 코멘트 | 필드별 말풍선 아이콘         | 편집 모드에서 각 필드 옆 코멘트 아이콘. 클릭 시 코멘트 입력 Popover |
| 초안 저장     | `Button` (secondary)         | Draft 상태로 저장, 나중에 제출 가능                                 |
| 신청 제출     | `Button` (primary)           | 확인 Dialog 후 신청 생성                                            |

**상태별 UI**:

| 상태                   | 표시                                                                         |
| ---------------------- | ---------------------------------------------------------------------------- |
| Loading                | Sheet 내 Skeleton (정보 카드 + 이력)                                         |
| Error (상세 로드 실패) | Sheet 내 Alert: "정보를 불러오지 못했습니다" + 닫기                          |
| 편집 중 + 저장         | 버튼 Spinner, 폼 비활성화                                                    |
| 신청 제출 성공         | Sheet 닫힘 + Toast: "변경 신청이 제출되었습니다 (REQ-XXXX)"                  |
| 초안 저장 성공         | Toast: "초안이 저장되었습니다. 신규 신청 화면에서 이어서 작성할 수 있습니다" |

**역할별 차이점**:

- **조회전용**: 편집 모드 전환 불가, 변경/삭제 신청 버튼 숨김
- **승인자**: 변경/삭제 신청 버튼 숨김 (승인자는 검토만)
- **신청자/표준 관리자**: 전체 기능 사용 가능

**AI Data Butler 통합**:

- **편집 모드 진입 시**: "이 필드를 수정하면 관련 용어 N건에 영향이 있습니다" 안내
- **정의 필드 편집 시**: AI가 기존 유사 정의와 비교하여 개선 제안 표시 (인라인 힌트)
- **삭제 신청 시**: AI가 영향도를 자동 분석하여 "영향 받는 시스템 추천 체크" 제안

---

### 5.6 신규 표준 신청

**목적**: 존재하지 않는 새로운 표준(단어/도메인/용어)을 신규 등록 신청
**진입 경로**: 사이드바 "신규 신청", 탐색기 "신규 신청" 버튼, AI 추천의 "그래도 신규 신청" 링크
**경로**: `/solutions/codex/standards/new`

**레이아웃**: PlatformShell > Main Content (전체 페이지 폼)

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "신규 표준 신청" [초안 목록 (2)] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 신청 유형 선택 │ │
│ │ ┌──────────┐ ┌──────────┐ ┌──────────┐ │ │
│ │ │ 표준단어 │ │ 표준도메인 │ │ 표준용어 │ │ │
│ │ │ 데이터 명칭│ │ 타입/길이 │ │ 비즈니스 │ │ │
│ │ │ 최소 단위 │ │ 정의 │ │ 용어 │ │ │
│ │ └──────────┘ └──────────┘ └──────────┘ │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌─────────────────────┬────────────────────────┐ │
│ │ 신청 폼 (좌측 60%) │ AI 추천 패널 (우측 40%)│ │
│ │ │ │ │
│ │ 표준단어* │ 🤖 AI Data Butler │ │
│ │ [고객유형___________]│ │ │
│ │ │ 유사 표준 3건 발견: │ │
│ │ 영문약어* │ │ │
│ │ [CUST_TP___________]│ 1. 고객구분 (92%) │ │
│ │ 힌트: 영문 대문자 │ CUST_DIV ● 기존 │ │
│ │ │ [상세] [사용하기] │ │
│ │ 영문명* │ │ │
│ │ [Customer Type_____]│ 2. 고객유형코드 (85%) │ │
│ │ │ CUST_TP_CD ● 기존 │ │
│ │ 도메인유형 │ [상세] [사용하기] │ │
│ │ [코드 ▾] │ │ │
│ │ │ 3. 거래유형 (71%) │ │
│ │ 정의* │ DEAL_TP ● 기존 │ │
│ │ [________________] │ [상세] [사용하기] │ │
│ │ [________________] │ │ │
│ │ │ ⚠️ "고객구분"과 92% │ │
│ │ 신청사유 │ 유사합니다. 기존 표준을 │ │
│ │ [________________] │ 확인해주세요. │ │
│ │ │ │ │
│ │ [초안 저장] [신청 제출]│ │ │
│ └─────────────────────┴────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**초안 시스템 (Draft)**:

- "초안 저장" 클릭 시 로컬/서버에 Draft 상태로 저장
- Topbar의 "초안 목록 (N)" 클릭 → Popover로 저장된 초안 목록 표시
- 초안 선택 시 폼에 자동 입력, 이어서 작성 가능
- 초안은 최대 10건까지 저장, 30일 후 자동 삭제
- 초안 목록에서 개별 삭제 가능

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트     | shadcn/ui 매핑                       | 설명                                                        |
| ------------ | ------------------------------------ | ----------------------------------------------------------- |
| 유형 선택    | 라디오 카드 (3개)                    | 표준단어/표준도메인/표준용어 선택. 선택에 따라 폼 필드 변경 |
| 신청 폼      | `Card` > `Input`/`Select`/`Textarea` | 유형별 필수/선택 필드 구성                                  |
| AI 추천 패널 | `Card` (우측 고정)                   | 폼 입력 시 실시간 유사 표준 추천                            |
| 중복 경고    | Alert (주황)                         | 80%+ 유사 표준 발견 시 표시                                 |
| 초안 저장    | `Button` (secondary)                 | Draft로 저장                                                |
| 신청 제출    | `Button` (primary)                   | 확인 Dialog 후 신청 생성                                    |

**유형별 폼 필드**:

| 필드            | 표준단어 | 표준도메인 |   표준용어   |
| --------------- | :------: | :--------: | :----------: |
| 표준명 (한국어) |   O\*    |    O\*     |     O\*      |
| 영문약어        |   O\*    |     -      |      -       |
| 영문명          |   O\*    |     -      |      -       |
| 물리명 (자동)   |    -     |     -      | O (readonly) |
| 도메인유형      |    O     |    O\*     |     O\*      |
| 데이터타입      |    -     |    O\*     |      -       |
| 데이터길이      |    -     |     O      |      -       |
| 인포타입        |    -     |     -      |     O\*      |
| 정의            |   O\*    |    O\*     |     O\*      |
| 신청사유        |    O     |     O      |      O       |

**상태별 UI**:

| 상태                 | 표시                                                                      |
| -------------------- | ------------------------------------------------------------------------- |
| Loading (AI 추천)    | AI 패널 Skeleton (3행)                                                    |
| Empty (AI 추천 없음) | "유사한 기존 표준이 없습니다. 새로운 표준을 등록하세요!"                  |
| 유효성 검증 실패     | 필수 필드 적색 border + 하단 오류 메시지                                  |
| 제출 중              | 버튼 Spinner + 폼 비활성화                                                |
| 제출 성공            | Dialog: "신청이 제출되었습니다 (REQ-XXXX). [대시보드로 이동] [계속 신청]" |
| 초안 저장 성공       | Toast: "초안이 저장되었습니다"                                            |

**역할별 차이점**: 이 화면은 신청 권한이 있는 역할에게만 접근 가능 (시스템관리자, 표준 관리자, 신청자).

**AI Data Butler 통합**:

- **실시간 유사 표준 추천**: 표준명 필드 입력 시 디바운스(300ms) 후 유사 표준 검색, 우측 패널 갱신
- **중복 경고**: 80%+ 유사 기존 표준 발견 시 주황 Alert, "사용하기" 클릭 시 해당 상세 Sheet 열림
- **영문약어 자동완성**: AI가 표준명으로부터 영문약어 추천 (예: "고객유형" → "CUST_TP")
- **정의 자동 생성**: 표준명과 도메인유형 기반으로 AI가 정의 초안 제안 (수락/수정 가능)

---

### 5.7 승인 워크벤치

**목적**: 승인 대기 건을 효율적으로 검토하고 승인/반려/피드백 처리하는 통합 작업 공간
**진입 경로**: 사이드바 "승인 워크벤치", 승인자 대시보드 "신청대기" 클릭
**경로**: `/solutions/codex/approvals`

**레이아웃**: PlatformShell > Main Content (좌우 분할: 목록 + 상세 패널)

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "승인 워크벤치" [일괄 승인] [일괄 반려] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌───────────────────────┬────────────────────────┐ │
│ │ 신청 목록 (좌 45%) │ 신청 상세 (우 55%) │ │
│ │ │ │ │
│ │ ┌────────────────┐ │ REQ-2026-0342 │ │
│ │ │ 🔍 [검색] [구분▾]│ │ 표준단어 | 변경 | 신청 │ │
│ │ │ [요청구분 ▾] │ │ 신청자: 이분석 │ │
│ │ └────────────────┘ │ 신청일: 2026-03-18 │ │
│ │ │ │ │
│ │ ☑ REQ-0342 ★ │ ── 변경 내용 비교 ── │ │
│ │ 표준단어 | 변경 │ ┌──────┬──────┬──────┐│ │
│ │ "고객유형코드" │ │ 항목 │현재값 │변경값 ││ │
│ │ 이분석 | 5일 전 │ ├──────┼──────┼──────┤│ │
│ │ │ │ 정의 │고객의 │고객을 ││ │
│ │ ☐ REQ-0341 │ │ │유형을 │유형별 ││ │
│ │ 표준용어 | 신규 │ │ │구분 │로 분류││ │
│ │ "결제금액" │ └──────┴──────┴──────┘│ │
│ │ 김개발 | 3일 전 │ │ │
│ │ │ ── 신청 사유 ── │ │
│ │ ☐ REQ-0340 │ "정의가 모호하여 구체적│ │
│ │ 표준도메인 | 신규 │ 으로 변경 신청합니다" │ │
│ │ "금액\_15_2" │ │ │
│ │ 박분석 | 1일 전 │ ── 인라인 코멘트 ── │ │
│ │ │ 💬 정의: "유형별로 │ │
│ │ ☐ REQ-0339 │ 분류"보다 "유형을 │ │
│ │ 표준단어 | 삭제 │ 구분하는 코드"가 │ │
│ │ "거래유형" │ 더 적절해 보입니다. │ │
│ │ 최영업 | 오늘 │ [코멘트 추가] │ │
│ │ │ │ │
│ │ │ ── 처리 이력 ── │ │
│ │ │ ● 03-18 [신청] 이분석 │ │
│ │ │ │ │
│ │ │ ── 처리 ── │ │
│ │ │ 처리사유\*: │ │
│ │ │ [________________] │ │
│ │ │ │ │
│ │ │ [승인] [반려] [검토요청] │ │
│ │ │ │ │
│ └───────────────────────┴────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트         | shadcn/ui 매핑                    | 설명                                                          |
| ---------------- | --------------------------------- | ------------------------------------------------------------- |
| 좌측 목록        | 커스텀 리스트 (체크박스 포함)     | 필터링 + 검색 지원, 선택 시 우측 상세 갱신                    |
| 필터             | `Input` (search) + `Select` (2개) | 구분(표준단어/도메인/용어/공통코드), 요청구분(신규/변경/삭제) |
| 목록 항목        | 커스텀 카드                       | 신청번호, 유형 Badge, 요청구분, 대상명, 신청자, 경과일        |
| 경과일 강조      | 색상 표시                         | 7일+ 적색, 3-7일 주황, 3일 미만 기본                          |
| 우측 상세        | 리사이즈 가능 패널                | 신청 개요, 변경 비교, 사유, 코멘트, 이력, 처리                |
| 변경 비교 테이블 | `Table` (3-column diff)           | 현재값/변경요청값 비교, 변경 셀 하이라이트                    |
| 인라인 코멘트    | Popover / 커스텀                  | 특정 필드에 대한 의견 작성, 리뷰 시 활용                      |
| 처리 버튼        | `Button` (3종)                    | 승인(primary), 반려(destructive), 검토요청(secondary)         |
| 일괄 처리        | Topbar `Button`                   | 체크박스 선택된 항목 일괄 승인/반려 + 사유 입력 Dialog        |

**핵심 비즈니스 규칙**:

- 본인 신청 건은 목록에서 자동 제외 (신청자/승인자 분리 원칙)
- 모든 처리에 처리사유 입력 필수
- 삭제 신청 건은 영향도 평가 내용이 상세 패널에 추가 표시

**상태별 UI**:

| 상태                 | 표시                                                                                |
| -------------------- | ----------------------------------------------------------------------------------- |
| Loading              | 좌측 리스트 Skeleton, 우측 패널 Skeleton                                            |
| Empty (대기 건 없음) | "처리할 승인 건이 없습니다" 아이콘 + 메시지 (좌측 전체)                             |
| 우측 미선택          | "좌측에서 신청 건을 선택하세요" 안내 텍스트                                         |
| 처리 중              | 버튼 Spinner + 비활성화                                                             |
| 처리 성공            | Toast: "{처리유형} 처리되었습니다" + 좌측 목록에서 해당 건 제거 + 다음 건 자동 선택 |
| Error                | Alert: "처리에 실패했습니다. [다시 시도]"                                           |

**역할별 차이점**: 이 화면은 시스템관리자와 검토/승인자에게만 접근 가능.

**AI Data Butler 통합**:

- **유사 표준 경고**: 신규 신청 건에 대해 "기존 '고객유형코드'와 85% 유사합니다" 인라인 안내
- **승인 추천**: AI가 신청 내용을 분석하여 "명명 규칙 준수 확인됨", "필수 항목 충족" 등 자동 체크리스트 표시
- **반려 사유 자동 완성**: 반려 버튼 클릭 시 AI가 빈번한 반려 사유 템플릿 추천

---

### 5.8 검증 대시보드

**목적**: 표준 검증 결과 요약 및 검증 실행/이력 관리
**진입 경로**: 사이드바 "검증 대시보드", 거버넌스 포털 "검증 위반" 링크
**경로**: `/solutions/codex/validations`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "검증 대시보드" [전체 검증 실행] [엑셀 다운] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│ │총 위반│ │명명규칙│ │금칙어 │ │중복등록│ │
│ │ 127건 │ │ 54건 │ │ 31건 │ │ 42건 │ │
│ │↓-23 │ │42.5% │ │24.4% │ │33.1% │ │
│ └──────┘ └──────┘ └──────┘ └──────┘ │
│ │
│ ┌─────────────────────┬────────────────────────┐ │
│ │ 월별 위반 추이 (좌측) │ 검증 규칙별 현황 (우측) │ │
│ │ │ │ │
│ │ [바 차트, 6개월] │ 규칙명 │ 위반 │ 심각도│ │
│ │ │ 영문약어│ 54 │ 높음● │ │
│ │ │ 중복검출│ 42 │ 중간◐ │ │
│ │ │ 금칙어 │ 31 │ 중간◐ │ │
│ │ │ 필수항목│ 16 │ 낮음○ │ │
│ │ │ 길이제한│ 8 │ 낮음○ │ │
│ └─────────────────────┴────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 최근 검증 이력 │ │
│ │ 실행일시 │대상범위│규칙수│검사│위반│결과 │실행│ │
│ │ 03-20 09:00 │전체 │5 │7524│127│위반● │시스│ │
│ │ 03-19 09:00 │전체 │5 │7480│131│위반● │시스│ │
│ │ 03-18 14:30 │단어 │3 │2847│ 54│일부◐ │김관│ │
│ │ [상세 →] │ │ │ │ │ │ │ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트       | shadcn/ui 매핑         | 설명                                                                         |
| -------------- | ---------------------- | ---------------------------------------------------------------------------- |
| Stat 카드      | `Card` (4-column grid) | 총 위반, 명명규칙, 금칙어, 중복등록 건수. 클릭 시 검증 상세로 이동 (해당 탭) |
| 위반 추이 차트 | `Card` > 바 차트       | 6개월 월별 위반 건수 추이                                                    |
| 규칙별 현황    | `Card` > `Table`       | 규칙명, 위반수, 심각도 Badge, "상세" 링크                                    |
| 검증 이력      | `Card` > `Table`       | 실행일시, 대상범위, 규칙수, 검사항목수, 위반건수, 결과 Badge, 실행자         |
| 전체 검증 실행 | `Button` (primary)     | 확인 Dialog 후 전체 검증 실행                                                |
| 엑셀 다운로드  | `Button` (secondary)   | 현재 위반 목록 엑셀 내보내기                                                 |

**상태별 UI**:

| 상태                | 표시                                                      |
| ------------------- | --------------------------------------------------------- |
| Loading             | Stat 카드 Skeleton, 차트 Skeleton, 테이블 Skeleton        |
| Empty (검증 미실행) | "아직 검증을 실행하지 않았습니다. [전체 검증 실행하기 →]" |
| 검증 실행 중        | Progress bar + "검증 진행 중... (3,421 / 7,524)"          |
| Error               | Alert: "검증 데이터를 불러오지 못했습니다"                |

**역할별 차이점**:

- **시스템관리자/표준 관리자**: "전체 검증 실행" 버튼 표시
- **기타 역할**: 조회만 가능, 실행 버튼 숨김

**AI Data Butler 통합**:

- **위반 패턴 분석**: "이번 달 명명규칙 위반 54건 중 42건이 영문약어 소문자 사용입니다. 일괄 시정을 권장합니다" 인사이트 카드
- **자동 시정 추천**: 각 위반 유형에 대해 AI가 시정 방안 요약 제공

---

### 5.9 검증 상세

**목적**: 특정 검증 실행의 위반 항목 목록을 확인하고 일괄 시정 신청
**진입 경로**: 검증 대시보드의 Stat 카드 클릭, 규칙별 "상세" 링크, 이력 행 "상세" 링크
**경로**: `/solutions/codex/validations/[executionId]`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "검증 상세" [일괄 시정 신청] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌───────┬───────┬──────┬──────┬───────┬──────┐ │
│ │전체 │명명규칙│금칙어│중복 │필수항목│길이 │ │
│ │(127) │(54) │(31) │(42) │(16) │(8) │ │
│ └───────┴───────┴──────┴──────┴───────┴──────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ [대상유형 ▾] [심각도 ▾] 🔍 [검색___________] │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ ☐ │ 대상 │ 항목명 │위반규칙│위반내용│심각│상태│ │
│ ├───┼──────┼───────────┼──────┼──────┼───┼───┤ │
│ │ ☑ │단어● │ payAmt │명명 │→PAY\_ │높●│미처│ │
│ │ │ │ │규칙 │AMT │음 │리 │ │
│ │ ☑ │용어◐ │ custNm │명명 │→CUST │높●│미처│ │
│ │ │ │ │규칙 │_NM │음 │리 │ │
│ │ ☐ │단어○ │ 코드명칭 │금칙어│"코드"│중◐│시정│ │
│ │ │ │ │ │→"유형"│간 │중 │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ◀ 1 2 3 ... 13 ▶ 10건/페이지 총 127건 │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트       | shadcn/ui 매핑                    | 설명                                                             |
| -------------- | --------------------------------- | ---------------------------------------------------------------- |
| 규칙 탭        | `Tabs`                            | 전체/명명규칙/금칙어/중복/필수항목/길이제한. 각 탭에 건수 Badge  |
| 필터           | `Select` (2개) + `Input` (search) | 대상유형, 심각도, 키워드                                         |
| 위반 목록      | `Table` (체크박스 포함)           | 대상 Badge, 항목명, 위반규칙, 위반내용, 심각도 Badge, 상태 Badge |
| 행 클릭        | → 표준 상세 Sheet 열림            | 해당 표준의 상세 보기 (5.5)                                      |
| 일괄 시정 신청 | `Button` (primary)                | 체크박스 선택 → 변경 신청 자동 생성                              |
| 페이지네이션   | 커스텀 Pagination                 | 10건/페이지                                                      |

**상태별 UI**:

| 상태              | 표시                                                              |
| ----------------- | ----------------------------------------------------------------- |
| Loading           | 테이블 Skeleton (5행)                                             |
| Empty (위반 없음) | "위반 항목이 없습니다. 모든 표준이 규칙을 준수합니다" 성공 아이콘 |
| Error             | Alert: "위반 목록을 불러오지 못했습니다"                          |

**역할별 차이점**:

- **시스템관리자/표준 관리자/신청자**: 일괄 시정 신청 가능
- **조회전용**: 일괄 시정 버튼 숨김, 조회만 가능

**AI Data Butler 통합**: 위반 항목별로 AI가 시정 방안을 인라인 제안 (예: "payAmt → PAY_AMT로 변경 권장")

---

### 5.10 거버넌스 포털

**목적**: 조직 전체 표준 거버넌스 현황을 종합적으로 모니터링
**진입 경로**: 사이드바 "거버넌스 포털"
**경로**: `/solutions/codex/governance`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "거버넌스 포털" 기준일: 2026-03-20 │
│ [PDF 리포트] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 준수율 게이지 (3개 SVG 원형) │ │
│ │ │ │
│ │ ┌──────┐ ┌──────┐ ┌──────┐ │ │
│ │ │ 87% │ │ 92% │ │ 79% │ │ │
│ │ │표준단어│ │표준도메│ │표준용어│ │ │
│ │ │▲2.3% │ │▲1.1% │ │▼0.4% │ │ │
│ │ └──────┘ └──────┘ └──────┘ │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│ │처리율 │ │승인소요│ │반려율 │ │검증위반│ │
│ │94.2% │ │ 2.3일 │ │ 8.7% │ │ 127건│ │
│ └──────┘ └──────┘ └──────┘ └──────┘ │
│ │
│ ┌─────────────────────┬────────────────────────┐ │
│ │ 월별 표준화율 추이 │ 부서별 준수율 랭킹 │ │
│ │ (3색 그룹 바 차트) │ │ │
│ │ │ IT개발팀 ████ 95% │ │
│ │ [6개월 추이] │ 데이터팀 ███░ 91% │ │
│ │ │ 경영지원팀 ██░░ 85% │ │
│ │ │ 영업팀 █░░░ 72% │ │
│ └─────────────────────┴────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 미준수 항목 Top 10 │ │
│ │ 순위│항목명 │유형 │위반사유│부서 │미준수기간│ │
│ │ 1 │payAmt │단어● │명명위반│영업 │ 45일 │ │
│ │ 2 │custNm │용어◐ │명명위반│개발 │ 30일 │ │
│ │ ...│ │ │ │ │ │ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트      | shadcn/ui 매핑               | 설명                                                                     |
| ------------- | ---------------------------- | ------------------------------------------------------------------------ |
| 준수율 게이지 | SVG 원형 (커스텀)            | 3개 표준 유형별 준수율 %, 전월 대비 증감                                 |
| KPI 카드      | `Card` (4-column grid)       | 처리율, 승인소요, 반려율, 검증위반. "검증위반" 클릭 → 검증 대시보드 이동 |
| 표준화율 추이 | `Card` > 3색 그룹 바 차트    | 6개월 추이 (단어=파랑, 도메인=녹색, 용어=주황)                           |
| 부서별 랭킹   | `Card` > Progress bar 리스트 | 준수율 기준 내림차순, 색상 코딩 (90%+녹, 80-90%파랑, 70-80%주황, 70%-적) |
| 미준수 Top 10 | `Card` > `Table`             | 순위, 항목명, 유형 Badge, 위반사유, 소속부서, 미준수기간 (mono)          |
| PDF 리포트    | `Button` (secondary)         | 현재 거버넌스 현황 PDF 내보내기                                          |

**상태별 UI**:

| 상태                | 표시                                                                 |
| ------------------- | -------------------------------------------------------------------- |
| Loading             | 게이지 Skeleton, KPI Skeleton, 차트 Skeleton                         |
| Empty (데이터 부족) | "충분한 거버넌스 데이터가 없습니다. 표준 등록 후 확인할 수 있습니다" |
| Error               | Alert: "거버넌스 데이터를 불러오지 못했습니다"                       |

**역할별 차이점**: 시스템관리자, 검토/승인자, 표준 관리자만 접근 가능.

**AI Data Butler 통합**:

- **인사이트 요약**: "표준용어 준수율이 3개월 연속 하락 중입니다. 주요 원인은 영업팀의 비표준 용어 사용입니다" AI 요약 카드
- **개선 제안**: "이번 달 검증 위반 127건 중 42건은 자동 시정 가능합니다. [일괄 시정 제안 보기]"

---

### 5.11 감사 추적

**목적**: 모든 표준 변경 이력을 시간순으로 조회하고 추적
**진입 경로**: 사이드바 "감사 추적", 대시보드 "전체 이력 보기" 링크
**경로**: `/solutions/codex/audit`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "감사 추적" │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 고급 필터 │ │
│ │ [대상유형 ▾] [키워드____] [기간 from~to] │ │
│ │ [작업유형 ▾] [처리자____] [검색] [초기화] │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 일시 │ 대상 │ 작업 │상태변경│처리│ │
│ ├──────────────┼────────────┼─────┼───────┼───┤ │
│ │ 03-20 09:30 │ 고객번호 │승인● │신청→ │김검│ │
│ │ │ 표준용어◐ │ │ 승인 │토 │ │
│ ├──────────────┼────────────┼─────┼───────┼───┤ │
│ │ 03-20 09:15 │ 결제수단코드│신청○ │미등록│나신│ │
│ │ │ 표준용어◐ │ │→신청 │청 │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ── 행 클릭 시 하단에 상세 타임라인 패널 확장 ── │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 📋 "고객번호" 전체 이력 │ │
│ │ │ │
│ │ ● 2026-03-20 [승인] 김검토 (승인자) │ │
│ │ 신청→승인. 사유: "정의 적절함" │ │
│ │ 메타: REQ-2026-0342, CUST_NO │ │
│ │ │ │
│ │ ● 2026-03-18 [신청] 이분석 (신청자) │ │
│ │ 미등록→신청. 사유: "신규 표준 등록" │ │
│ │ │ │
│ │ ● 2025-08-15 [승인] 박관리 (관리자) │ │
│ │ 신청→승인. 최초 등록 │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ◀ 1 2 3 ... 5 ▶ 10건/페이지 총 48건 │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트      | shadcn/ui 매핑                       | 설명                                                                       |
| ------------- | ------------------------------------ | -------------------------------------------------------------------------- |
| 고급 필터     | `Card` > `Select`/`Input`/DatePicker | 대상유형, 키워드, 기간, 작업유형, 처리자                                   |
| 감사 테이블   | `Table`                              | 일시(mono), 대상(이름+유형Badge), 작업(Badge), 상태변경, 처리자(이름+역할) |
| 행 클릭 확장  | Collapsible 패널                     | 해당 표준의 전체 타임라인 표시                                             |
| 상세 타임라인 | 커스텀 타임라인                      | 시간순 이력, 각 이벤트의 메타데이터(신청ID, 약어 등) 포함                  |
| 페이지네이션  | 커스텀 Pagination                    | 10건/페이지                                                                |

**상태별 UI**:

| 상태              | 표시                                              |
| ----------------- | ------------------------------------------------- |
| Loading           | 필터 Skeleton, 테이블 Skeleton (5행)              |
| Empty (결과 없음) | "조건에 맞는 감사 이력이 없습니다. [필터 초기화]" |
| Error             | Alert: "감사 이력을 불러오지 못했습니다"          |

**역할별 차이점**: 조회전용 사용자는 접근 불가. 나머지 역할은 동일하게 조회 가능.

**AI Data Butler 통합**: 필터 영역에 "최근 반려된 건 보기", "이번 주 승인 이력" 등 빠른 필터 추천.

---

### 5.12 공통코드 관리

**목적**: 조직 공용 참조 코드(코드그룹/코드)의 CRUD 관리. 모든 변경은 거버넌스 파이프라인을 통해 처리
**진입 경로**: 사이드바 "관리 > 공통코드 관리"
**경로**: `/solutions/codex/admin/common-codes`

**레이아웃**: PlatformShell > Main Content (좌우 분할)

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "공통코드 관리" [코드그룹 추가] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌─────────────────┬──────────────────────────────┐ │
│ │ 코드그룹 (좌320px)│ 상세 코드 (우) │ │
│ │ │ │ │
│ │ 🔍 [검색_______]│ 거래유형 (TRADE_TYPE) 5건 │ │
│ │ │ │ │
│ │ ● 거래유형 5건│ [코드 추가] │ │
│ │ 금결제방법 3건│ │ │
│ │ 계좌유형 4건│ No│코드 │코드명│설명│상태│등록│ │
│ │ 상품구분 6건│ 1 │TRD001│매매 │... │기존│3/1│ │
│ │ 통화코드 8건│ 2 │TRD002│위탁 │... │기존│3/1│ │
│ │ ... │ 3 │TRD003│펀드 │... │신청│3/18│ │
│ │ │ 4 │TRD004│채권 │... │기존│3/1│ │
│ │ │ 5 │TRD005│파생 │... │기존│3/1│ │
│ │ │ │ │
│ └─────────────────┴──────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트         | shadcn/ui 매핑               | 설명                                              |
| ---------------- | ---------------------------- | ------------------------------------------------- |
| 코드그룹 목록    | `ScrollArea` > 커스텀 리스트 | 좌측 패널, 검색 지원, 선택 시 우측 코드 목록 갱신 |
| 코드그룹 추가    | `Dialog`                     | 그룹코드, 그룹명 입력                             |
| 상세 코드 테이블 | `Table`                      | 코드, 코드명, 설명, 상태 Badge, 등록일            |
| 코드 추가        | `Dialog`                     | 코드, 코드명, 설명 입력 → 거버넌스 신청 자동 생성 |

**핵심 비즈니스 규칙**: CRUD 버튼 클릭 시 직접 수정이 아닌 거버넌스 요청(신청) 생성. '코드(공통코드)' 도메인유형이 먼저 승인되어 있어야 코드 등록 가능.

**상태별 UI**:

| 상태              | 표시                                                |
| ----------------- | --------------------------------------------------- |
| Loading           | 좌측 리스트 Skeleton, 우측 테이블 Skeleton          |
| Empty (그룹 없음) | "등록된 코드그룹이 없습니다. [코드그룹 추가하기 →]" |
| 우측 미선택       | "좌측에서 코드그룹을 선택하세요"                    |
| Error             | Alert 표시                                          |

**역할별 차이점**: 시스템관리자와 표준 관리자만 접근 가능. 표준 관리자는 CRUD 전체 가능.

**AI Data Butler 통합**: 코드 추가 시 유사 코드 중복 검사 인라인 안내.

---

### 5.13 공통코드 조회

**목적**: 모든 사용자가 접근 가능한 공통코드 읽기전용 검색 화면
**진입 경로**: 사이드바 "공통코드 조회"
**경로**: `/solutions/codex/common-codes`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "공통코드 조회" │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 🔍 [코드그룹명, 그룹코드, 코드명, 코드로 검색] │ │
│ │ [도메인유형 ▾] │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ No│코드그룹명│그룹코드 │코드 │코드명│설명│사용│ │
│ ├──┼────────┼──────────┼──────┼────┼───┼───┤ │
│ │ 1│거래유형 │TRADE_TYPE│TRD001│매매│... │Y● │ │
│ │ 2│거래유형 │TRADE_TYPE│TRD002│위탁│... │Y● │ │
│ │ 3│결제방법 │PAY_METHOD│PAY001│카드│... │Y● │ │
│ │ 4│결제방법 │PAY_METHOD│PAY002│현금│... │N○ │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ◀ 1 2 ... 8 ▶ 10건/페이지 총 76건 │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트     | shadcn/ui 매핑              | 설명                                                     |
| ------------ | --------------------------- | -------------------------------------------------------- |
| 검색 바      | `Input` (search) + `Select` | 키워드 검색, 도메인유형 필터                             |
| 코드 테이블  | `Table`                     | 코드그룹명, 그룹코드, 코드, 코드명, 설명, 사용여부 Badge |
| 페이지네이션 | 커스텀 Pagination           | 10건/페이지                                              |

**상태별 UI**: 표준 패턴 (Loading Skeleton, Empty State, Error Alert)

**역할별 차이점**: 모든 역할이 동일하게 조회 가능 (읽기전용).

**AI Data Butler 통합**: 검색 자동완성 (코드그룹명, 코드명 기반).

---

### 5.14 사용자 관리

**목적**: 시스템 사용자 계정의 생성, 수정, 비활성화 관리
**진입 경로**: 사이드바 "관리 > 사용자 관리"
**경로**: `/solutions/codex/admin/users`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "사용자 관리" [사용자 추가] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────┐ ┌──────┐ ┌──────┐ │
│ │전체 │ │관리자 │ │일반 │ │
│ │ 24명 │ │ 3명 │ │ 21명 │ │
│ └──────┘ └──────┘ └──────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 🔍 [키워드 검색_________] [역할 ▾] │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ No│사용자ID│이름 │역할 │부서│이메일│최종로│상태│액│ │
│ ├──┼──────┼─────┼─────┼───┼────┼────┼───┼──┤ │
│ │ 1│admin │김관리│관리자│IT │k@..│3/20│활성│✏🗑│ │
│ │ 2│review│박검토│일반 │데이│p@..│3/19│활성│✏🗑│ │
│ │ 3│user1 │이신청│일반 │영업│l@..│3/18│비활│✏🗑│ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트      | shadcn/ui 매핑     | 설명                                             |
| ------------- | ------------------ | ------------------------------------------------ |
| Stat 카드     | `Card` (3-column)  | 전체/관리자/일반 사용자 수                       |
| 검색/필터     | `Input` + `Select` | 키워드 검색, 역할 필터                           |
| 사용자 테이블 | `Table`            | 계정 정보, 상태 Badge, 액션 (수정/삭제)          |
| 사용자 추가   | `Dialog`           | 사용자ID*, 이름*, 비밀번호\*, 역할, 부서, 이메일 |
| 수정          | `Dialog`           | 기존 정보 사전 입력된 폼                         |
| 삭제          | 확인 `Dialog`      | "정말 삭제하시겠습니까?" 확인                    |

**상태별 UI**: 표준 패턴

**역할별 차이점**: 시스템관리자만 접근 가능.

**AI Data Butler 통합**: 미적용 (관리 기능).

---

### 5.15 권한 관리

**목적**: 역할별 메뉴 접근 권한 및 CRUD 권한 설정
**진입 경로**: 사이드바 "관리 > 권한 관리"
**경로**: `/solutions/codex/admin/permissions`

**레이아웃**: PlatformShell > Main Content (좌우 분할)

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "권한 관리" [저장] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌────────┬──────────┬──────────┬────────┬────────┐ │
│ │시스템관리│검토/승인자│표준 관리자│ 신청자 │조회전용│ Tabs │
│ └────────┴──────────┴──────────┴────────┴────────┘ │
│ │
│ ┌─────────────────┬──────────────────────────────┐ │
│ │ 메뉴 트리 (좌) │ 권한 상세 (우) │ │
│ │ │ │ │
│ │ ☑ 대시보드 │ 접근 가능: 12/15개 메뉴 │ │
│ │ ▼ 표준 관리 │ │ │
│ │ ☑ 표준 탐색기 │ 메뉴 │조회│등록│수정│삭제│ │
│ │ ☑ 신규 신청 │ 대시보드 │ ☑ │ ☐ │ ☐ │ ☐ │ │
│ │ ☑ 공통코드조회 │ 표준 탐색기 │ ☑ │ ☑ │ ☐ │ ☐ │ │
│ │ ▼ 거버넌스 │ 신규 신청 │ ☑ │ ☑ │ ☐ │ ☐ │ │
│ │ ☐ 승인 워크벤치│ 승인 워크벤치│ ☐ │ ☐ │ ☐ │ ☐ │ │
│ │ ☑ 감사 추적 │ ... │ │ │ │ │ │
│ │ ... │ │ │
│ └─────────────────┴──────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트  | shadcn/ui 매핑         | 설명                                                           |
| --------- | ---------------------- | -------------------------------------------------------------- |
| 역할 탭   | `Tabs`                 | 5개 역할(시스템관리/검토승인/표준 관리자/신청자/조회전용) 전환 |
| 메뉴 트리 | 커스텀 Tree (체크박스) | 계층 구조, 상위 선택 시 하위 자동 선택                         |
| 권한 상세 | `Table` (체크박스)     | 메뉴별 CRUD 체크박스                                           |
| 저장      | `Button` (primary)     | 변경사항 저장, 확인 Toast                                      |

**상태별 UI**: 표준 패턴

**역할별 차이점**: 시스템관리자만 접근 가능.

**AI Data Butler 통합**: 미적용 (관리 기능).

---

### 5.16 코드 관리 (시스템)

**목적**: 도메인유형, 표준상태코드, 요청구분코드 등 시스템 기준 코드 관리
**진입 경로**: 사이드바 "관리 > 코드 관리"
**경로**: `/solutions/codex/admin/system-codes`

**레이아웃**: PlatformShell > Main Content

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "시스템 코드 관리" [코드 추가] │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────────────┬──────────────┬──────────────┐ │
│ │ 도메인유형 │ 표준상태코드 │ 요청구분코드 │ │
│ └──────────────┴──────────────┴──────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ No│보호│코드│코드명 │설명 │변경│등록일│ │
│ ├──┼───┼───┼──────┼───────────┼───┼─────┤ │
│ │ 1│🔒 │STR│문자열 │문자열 데이터│불가│1/1 │ │
│ │ 2│🔒 │NUM│숫자 │숫자 데이터 │불가│1/1 │ │
│ │ 3│🔒 │DT │일자 │일자 데이터 │불가│1/1 │ │
│ │ 4│🔒 │CD │코드 │코드 데이터 │불가│1/1 │ │
│ │ 5│ │LOB│대용량 │대용량 데이터│가능│3/15 │ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트    | shadcn/ui 매핑 | 설명                                                   |
| ----------- | -------------- | ------------------------------------------------------ |
| 카테고리 탭 | `Tabs`         | 도메인유형/표준상태코드/요청구분코드                   |
| 코드 테이블 | `Table`        | 보호 Badge, 코드, 코드명, 설명, 변경가능 Badge, 등록일 |
| 코드 추가   | `Dialog`       | 비보호 코드만 추가 가능                                |

**핵심 비즈니스 규칙**: 보호 코드(STR, NUM, DT, CD 등)는 수정/삭제 불가. 워크플로우 무결성 보장.

**상태별 UI**: 표준 패턴

**역할별 차이점**: 시스템관리자만 접근 가능.

**AI Data Butler 통합**: 미적용 (관리 기능).

---

### 5.17 DB 연결 설정

**목적**: 표준관리 DB 접속 정보 및 SSH 터널링 설정 관리
**진입 경로**: 사이드바 "관리 > DB 연결 설정", 로그인 화면 "환경설정" 링크
**경로**: `/solutions/codex/admin/db-settings`

**레이아웃**: PlatformShell > Main Content (카드 3개 세로 배치)

```markdown
┌──────────────────────────────────────────────────────┐
│ Topbar: "DB 연결 설정" │
├──────────────────────────────────────────────────────┤
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 카드 1: 표준관리 DB 접속 정보 │ │
│ │ │ │
│ │ DB 유형 [Oracle ▾] 호스트 [192.168.1.100] │ │
│ │ 포트 [1521] DB/SID [METAFORGE] │ │
│ │ 사용자명 [meta_admin] 비밀번호 [••••••] │ │
│ │ 스키마 [META_STD] 문자셋 [UTF-8 ▾] │ │
│ │ │ │
│ │ [접속 테스트] ● 성공: Oracle 19c, 23ms │ │
│ │ [저장] │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 카드 2: SSH 터널링 │ │
│ │ │ │
│ │ SSH 터널링 사용 [토글 ON/OFF] │ │
│ │ SSH 호스트 [___] SSH 포트 [22] │ │
│ │ SSH 사용자명 [___] 인증방식 [비밀번호 ▾] │ │
│ │ SSH 비밀번호 [••••••] │ │
│ │ │ │
│ │ [터널링 테스트] [저장] │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ ┌──────────────────────────────────────────────┐ │
│ │ 카드 3: 접속정보 관리 │ │
│ │ │ │
│ │ [XML 내보내기] [XML 가져오기] │ │
│ └──────────────────────────────────────────────┘ │
│ │
└──────────────────────────────────────────────────────┘
```

**주요 컴포넌트 & 인터랙션**:

| 컴포넌트    | shadcn/ui 매핑                   | 설명                                                             |
| ----------- | -------------------------------- | ---------------------------------------------------------------- |
| DB 접속 폼  | `Card` > `Input`/`Select`        | DB유형, 호스트, 포트, DB/SID, 사용자명, 비밀번호, 스키마, 문자셋 |
| 접속 테스트 | `Button` (secondary)             | 성공 시 녹색 Badge (DB 버전, 응답시간), 실패 시 적색 Alert       |
| SSH 설정    | `Card` > 토글 + `Input`/`Select` | SSH 터널링 ON/OFF, 호스트, 포트, 인증방식                        |
| XML 관리    | `Card` > `Button` (2개)          | 내보내기/가져오기, 가져오기 시 파일 업로드 Dialog                |

**상태별 UI**:

| 상태           | 표시                                                        |
| -------------- | ----------------------------------------------------------- |
| Loading        | 카드 내 필드 Skeleton                                       |
| 접속 테스트 중 | 버튼 Spinner + "테스트 중..."                               |
| 테스트 성공    | 녹색 Badge: "Oracle 19c, 응답시간 23ms"                     |
| 테스트 실패    | 적색 Alert: "연결에 실패했습니다. 접속 정보를 확인해주세요" |
| 저장 성공      | Toast: "접속 정보가 저장되었습니다"                         |

**역할별 차이점**: 시스템관리자만 접근 가능.

**AI Data Butler 통합**: 미적용 (인프라 설정).

---

## 6. AI Data Butler UX 명세

### 6.1 전체 통합 전략

AI Data Butler는 별도 페이지가 아닌, 각 화면의 맥락에 통합된 Co-pilot으로 동작한다. 기존 `ai-term-suggest.html`의 독립 화면 패턴을 폐지하고, 다음 3가지 형태로 분산 통합한다.

| 통합 형태            | 설명                                         | 적용 화면                                             |
| -------------------- | -------------------------------------------- | ----------------------------------------------------- |
| **인라인 자동완성**  | 입력 필드에서 실시간 유사 표준 추천 드롭다운 | 통합 탐색기 검색, 신규 신청 폼, Command Palette       |
| **사이드 패널 추천** | 폼 우측에 유사 표준 목록 + 매칭 근거 표시    | 신규 표준 신청, 상세 Sheet 편집 모드                  |
| **맥락형 인사이트**  | 현재 화면 데이터에 기반한 AI 분석 결과       | 승인 워크벤치, 검증 대시보드, 거버넌스 포털, 대시보드 |

### 6.2 화면별 AI 통합 포인트

| 화면              | AI 트리거                    | 표시 방식                        | 내용                                      |
| ----------------- | ---------------------------- | -------------------------------- | ----------------------------------------- |
| 통합 표준 탐색기  | 검색어 입력 (디바운스 300ms) | 검색 Input 하단 Command 드롭다운 | 유사 표준명/약어/물리명 자동완성          |
| 통합 표준 탐색기  | 검색 결과 Empty              | Empty State 내 추천 섹션         | "혹시 이것을 찾으셨나요?" 3건             |
| 상세 Sheet (편집) | 편집 모드 진입               | Sheet 내 인라인 힌트             | "이 필드 수정 시 관련 용어 N건 영향"      |
| 상세 Sheet (삭제) | 삭제 신청 시작               | Stepper 내 추천                  | 영향도 자동 분석, 대체 표준 추천          |
| 신규 표준 신청    | 표준명 입력 (디바운스 300ms) | 우측 사이드 패널                 | 유사 표준 3건 + 유사도% + 매칭 근거       |
| 신규 표준 신청    | 80%+ 유사 발견               | 폼 상단 주황 Alert               | 중복 경고 + 기존 표준 링크                |
| 신규 표준 신청    | 영문약어 입력                | Input 하단 힌트                  | AI 추천 약어 (예: "고객유형" → "CUST_TP") |
| 승인 워크벤치     | 신청 상세 조회               | 상세 패널 내 인라인 태그         | 유사 표준 경고, 명명규칙 체크 결과        |
| 승인 워크벤치     | 반려 버튼 클릭               | 사유 Textarea 상단               | 반려 사유 템플릿 추천                     |
| 검증 대시보드     | 페이지 로드                  | 인사이트 카드                    | 위반 패턴 분석 요약, 자동 시정 제안       |
| 검증 상세         | 위반 항목 조회               | 위반 내용 컬럼 인라인            | AI 시정 제안 (예: "payAmt → PAY_AMT")     |
| 거버넌스 포털     | 페이지 로드                  | AI 요약 카드                     | 준수율 추이 분석, 개선 제안               |
| 대시보드 (신청자) | 페이지 로드                  | 플로팅 버튼 Popover              | 맥락 기반 알림 ("피드백 수정 제안 1건")   |
| Command Palette   | Ctrl+K 검색                  | 검색 결과 내                     | 유사 표준 실시간 추천                     |

### 6.3 AI 추천 패널 상세 UX

신규 표준 신청 화면의 우측 사이드 패널 구성:

```markdown
┌────────────────────────────┐
│ 🤖 AI Data Butler │
│ │
│ 유사 표준 3건 발견: │
│ │
│ ┌────────────────────────┐ │
│ │ 1. 고객구분 │ │
│ │ CUST_DIV │ │
│ │ 유사도: ██████░░ 92% │ │
│ │ 상태: ● 기존 │ │
│ │ 유형: 표준단어 │ │
│ │ [상세 보기] [사용하기] │ │
│ └────────────────────────┘ │
│ │
│ ┌────────────────────────┐ │
│ │ 2. 고객유형코드 │ │
│ │ CUST_TP_CD │ │
│ │ 유사도: █████░░░ 85% │ │
│ │ 상태: ● 기존 │ │
│ │ 유형: 표준용어 │ │
│ │ [상세 보기] [사용하기] │ │
│ └────────────────────────┘ │
│ │
│ ── AI 매칭 근거 ── │
│ (선택 항목의 근거 표시) │
│ │
│ 음절 유사도 ████████ 100%│
│ 의미 유사도 ███████░ 95%│
│ 약어 패턴 ██████░░ 88%│
│ │
│ ⚠️ "고객구분"과 92% 유사 │
│ 합니다. 기존 표준을 확인해 │
│ 주세요. │
│ │
└────────────────────────────┘
```

**인터랙션**:

- "상세 보기": 해당 표준의 상세 Sheet 열림 (현재 폼 입력 유지)
- "사용하기": 기존 표준 채택, 신규 신청 폼 닫힘, 해당 표준 상세 Sheet 열림
- 유사도 색상: 80%+ 파랑, 50-80% 주황, 50%- 회색
- 80%+ 유사 시 주황 경고 Alert 자동 표시

### 6.4 AI 데이터 흐름

```markdown
사용자 입력 (검색어/표준명)
│
▼
디바운스 (300ms)
│
▼
GET /api/ai/suggest?keyword={input}&type={type}&limit=3
│
▼
┌─ 결과 있음 ─── 추천 패널/드롭다운 표시
│ │
│ └─ 80%+ 유사 ─── 중복 경고 Alert 표시
│
└─ 결과 없음 ─── "유사 표준 없음. 신규 등록을 진행하세요" 메시지
```

---

## 7. 공유 UX 패턴

### 7.1 Toast 알림

시스템 전역에서 사용하는 일시적 피드백 메시지.

| 유형    | 색상 | 예시                                     | 자동 닫힘 |
| ------- | ---- | ---------------------------------------- | --------- |
| Success | 녹색 | "변경 신청이 제출되었습니다 (REQ-XXXX)"  | 5초       |
| Info    | 파랑 | "초안이 저장되었습니다"                  | 5초       |
| Warning | 주황 | "유사 표준이 발견되었습니다"             | 수동 닫기 |
| Error   | 적색 | "처리에 실패했습니다. 다시 시도해주세요" | 수동 닫기 |

**위치**: 화면 우측 하단 고정, 최대 3개 스택
**컴포넌트**: shadcn/ui Toast/Sonner

### 7.2 Stepper (단계 진행)

삭제 영향도 평가 등 다단계 프로세스에 사용.

```markdown
[1. 영향 시스템] ─── [2. 영향도 수준] ─── [3. 대체 표준] ─── [4. 삭제 사유]
●완료 ●현재 ○미진행 ○미진행
```

**규칙**:

- 완료 단계: 녹색 체크 아이콘, 클릭 시 이전 단계 복귀 가능
- 현재 단계: 파랑 활성 표시
- 미진행 단계: 회색, 클릭 불가
- "이전" / "다음" 버튼으로 단계 이동
- 마지막 단계에서 "제출" 버튼

### 7.3 Sheet (사이드 패널)

표준 상세 보기, 인라인 편집에 사용하는 우측 슬라이드 패널.

**규격**:

- 너비: 640px (데스크톱), 전체 너비 (모바일)
- 진입: 좌측에서 우측으로 슬라이드 애니메이션
- 닫기: X 버튼, ESC 키, 오버레이 클릭
- 중첩: 최대 2단 (상세 Sheet 위에 확인 Dialog)
- 배경: 반투명 오버레이 (opacity 0.5)

**컴포넌트**: shadcn/ui Sheet (side: right)

### 7.4 Command Palette

전역 검색 + 빠른 네비게이션 + 액션 실행.

**규격**:

- 트리거: Ctrl+K / Cmd+K, 헤더 검색 아이콘
- 위치: 화면 중앙 상단, 오버레이 위
- 너비: 최대 640px
- 섹션: 최근 검색 / 빠른 이동 / 액션 / 표준 검색
- 검색: fuzzy matching, 실시간 결과 갱신
- 키보드: 화살표 키 이동, Enter 선택, ESC 닫기

**컴포넌트**: shadcn/ui Command

### 7.5 알림 센터

실시간 알림 수신 및 확인.

**트리거**: 헤더의 벨 아이콘 (미확인 건수 Badge)
**표시**: 헤더 하단 Popover (최대 높이 400px, 스크롤)

```markdown
┌────────────────────────────┐
│ 알림 [모두 읽음] │
├────────────────────────────┤
│ ● [승인] "고객번호" 승인됨 │
│ 김검토 | 5분 전 │
│ │
│ ● [피드백] "결제수단" 수정요청│
│ 박승인 | 1시간 전 │
│ │
│ ○ [신규신청] 승인 대기 1건 │
│ 이분석 | 어제 │
│ │
│ "전체 알림 보기 →" │
└────────────────────────────┘
```

**알림 유형**:

| 유형           | 대상 역할   | 내용                                             |
| -------------- | ----------- | ------------------------------------------------ |
| 신규 신청 도착 | 승인자      | "새 신청이 도착했습니다: {항목명} ({유형})"      |
| 승인 완료      | 신청자      | "'{항목명}' 신청이 승인되었습니다"               |
| 반려           | 신청자      | "'{항목명}' 신청이 반려되었습니다. 사유: {사유}" |
| 피드백 요청    | 신청자      | "'{항목명}'에 대한 수정 요청이 있습니다"         |
| 검증 완료      | 표준 관리자 | "표준 검증이 완료되었습니다. 위반 {N}건 발견"    |

### 7.6 필터/검색 패턴

모든 목록 화면에서 일관된 필터/검색 UX를 사용한다.

**검색 바 구조**:

```markdown
🔍 [키워드 검색_______________] [필터1 ▾] [필터2 ▾] ... [검색] [초기화]
```

**규칙**:

- 키워드 검색: 디바운스 300ms, 자동 검색 (별도 버튼 불필요)
- 드롭다운 필터: 선택 즉시 결과 갱신
- "초기화" 버튼: 모든 필터를 기본값으로 리셋
- 필터 상태: URL query param에 반영 (뒤로 가기/공유 가능)
- 활성 필터 표시: 필터 영역 하단에 선택된 필터 칩 표시 (제거 가능)

### 7.7 테이블 공통 패턴

| 패턴         | 설명                                                                                                          |
| ------------ | ------------------------------------------------------------------------------------------------------------- |
| 정렬         | 컬럼 헤더 클릭으로 정렬 (ASC ↔ DESC 토글)                                                                     |
| 페이지네이션 | 하단 좌측: "총 N건", 하단 우측: 페이지 네비게이션                                                             |
| 행 클릭      | 상세 보기 Sheet 또는 확장 패널                                                                                |
| 상태 Badge   | 녹색(기존/활성/승인), 주황(신청/피드백), 적색(반려/삭제/높음), 파랑(검토/시정중/중간), 회색(취소/미처리/낮음) |
| Mono 텍스트  | 영문약어, 물리명, 코드, 일시 등 기술 값에 monospace 폰트 적용                                                 |
| 말줄임       | 정의 등 긴 텍스트는 1줄 말줄임 + 호버 시 전체 표시                                                            |
| 체크박스     | 일괄 처리가 필요한 테이블에만 추가 (승인 워크벤치, 검증 상세)                                                 |

### 7.8 상태 Badge 색상 체계

| 색상 | CSS Token             | 적용 상태                                                             |
| ---- | --------------------- | --------------------------------------------------------------------- |
| 녹색 | `--color-success`     | 기존(BASELINE), 활성(ACTIVE), 승인(APPROVED), 완료(RESOLVED), 사용(Y) |
| 주황 | `--color-warning`     | 신청(PENDING), 피드백대기(FEEDBACK), 중간(MEDIUM)                     |
| 적색 | `--color-destructive` | 반려(REJECTED), 삭제(DELETED), 높음(HIGH), 비활성(INACTIVE)           |
| 파랑 | `--color-primary`     | 검토(REVIEW), 시정중(CORRECTING), 베타                                |
| 회색 | `--color-muted`       | 취소(CANCELLED), 미처리(UNRESOLVED), 낮음(LOW), 미사용(N)             |
| 보라 | `--color-accent`      | AI 관련 Badge, Data Butler 라벨                                       |

### 7.9 확인 Dialog 패턴

삭제, 취소, 신청 제출 등 중요 액션 전에 표시하는 확인 대화상자.

```markdown
┌────────────────────────────┐
│ ⚠️ 신청을 취소하시겠습니까? │
│ │
│ 취소된 건은 복구할 수 없으며,│
│ 감사 추적에 기록됩니다. │
│ │
│ [아니오] [취소 확인] │
└────────────────────────────┘
```

**규칙**:

- 파괴적 액션(삭제, 취소)은 적색 확인 버튼
- 일반 확인(신청 제출)은 파랑/녹색 확인 버튼
- ESC 키로 닫기 가능
- "아니오" 버튼이 항상 좌측 (실수 방지)

---

## 8. 접근성 & 반응형 고려사항

### 8.1 접근성 (a11y)

| 항목              | 기준                                 | 구현 방법                                         |
| ----------------- | ------------------------------------ | ------------------------------------------------- |
| 키보드 네비게이션 | 모든 인터랙티브 요소에 Tab 접근 가능 | `tabindex`, `focus-visible` 스타일                |
| 스크린 리더       | ARIA 라벨, role 속성 적용            | `aria-label`, `aria-describedby`, `role`          |
| 색상 대비         | WCAG 2.1 AA 기준 (4.5:1 이상)        | Tailwind CSS v4 테마 토큰 색상 검증               |
| 포커스 표시       | 포커스 링 시각적 표시                | `ring-2 ring-offset-2` Tailwind 유틸리티          |
| Badge 색상        | 색맹 사용자를 위한 텍스트 라벨 병행  | Badge에 항상 텍스트 라벨 포함 (색상만 의존 안 함) |
| 폼 에러           | 에러 메시지를 필드와 연결            | `aria-invalid`, `aria-errormessage`               |
| 모달/Sheet        | 포커스 트랩, ESC 닫기                | shadcn/ui 기본 제공                               |
| 테이블            | 헤더 셀에 `scope` 속성               | `<th scope="col">`                                |

### 8.2 반응형 브레이크포인트

Nexus PlatformShell의 반응형 브레이크포인트를 따른다.

| 브레이크포인트 | 너비         | 레이아웃 변경                                  |
| -------------- | ------------ | ---------------------------------------------- |
| Desktop (기본) | 1280px+      | 사이드바 260px + 메인 콘텐츠                   |
| Tablet         | 768px-1279px | 사이드바 접힘 (아이콘만), 햄버거 메뉴          |
| Mobile         | ~767px       | 사이드바 숨김 (모바일 드로어), Sheet 전체 너비 |

**반응형 처리 세부사항**:

| 컴포넌트                  | Desktop             | Tablet               | Mobile                           |
| ------------------------- | ------------------- | -------------------- | -------------------------------- |
| 사이드바                  | 260px 고정          | 아이콘 모드 (60px)   | 숨김, 햄버거 드로어              |
| Stat 카드                 | 4-column grid       | 2-column grid        | 1-column 스택                    |
| 좌우 분할 (승인 워크벤치) | 45/55 분할          | 40/60 분할           | 1-column 스택 (목록 → 상세 전환) |
| 좌우 분할 (공통코드 관리) | 320px/나머지        | 접힘/전체 전환       | 1-column 스택                    |
| Sheet                     | 640px 우측 슬라이드 | 480px                | 전체 너비                        |
| 테이블                    | 전체 컬럼           | 핵심 컬럼만 (스크롤) | 카드형 리스트 전환               |
| Command Palette           | 640px 중앙          | 480px                | 전체 너비 - 패딩                 |
| 차트                      | 기본 크기           | 축소                 | 스크롤 또는 심플 수치            |

### 8.3 다크 모드 지원

Nexus PlatformShell의 `next-themes` 기반 다크 모드를 활용한다.

- **기본값**: 시스템 모드 (OS 설정 따름)
- **전환**: 헤더의 ThemeToggle 컴포넌트
- **구현**: Tailwind CSS v4 `@theme inline` 블록에 정의된 CSS 변수 기반
- **Badge 색상**: 다크 모드에서도 가독성 유지 (밝기 조정)
- **차트 색상**: 다크 모드 전용 팔레트 제공
